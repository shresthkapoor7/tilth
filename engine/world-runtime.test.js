import test from 'node:test';
import assert from 'node:assert/strict';
import {GameRuntime,SAVE_KEY} from './runtime.js';
test('the world adventure keeps completed optional quests without auto-queuing another',()=>{let id=0;const game=new GameRuntime({automaticQuestFollowups:false,id:()=>String(++id)});game.requestQuest();const job=game.state.jobs[0];game.complete(job.id,{title:'Visit the inn',description:'Stop by the inn.',sourceEventIds:job.eventIds,objectives:[{type:'visit_room',target:'inn',description:'Visit the inn.'}]});game.acceptQuest(job.id);game.record('room_entered','inn','Visited inn');assert.equal(game.state.quests[0].status,'complete');assert.equal(game.state.jobs.filter(j=>j.kind==='quest').length,1)});
test('adding world data preserves a saved profile and existing journals',()=>{const data=new Map();const storage={getItem:key=>data.get(key),setItem:(key,value)=>data.set(key,value)};const before=new GameRuntime({storage});before.record('room_entered','inn','An earlier visit');before.state.worldSimulation={id:'existing-world',version:1};before.changed();const after=new GameRuntime({storage,automaticQuestFollowups:false});assert.equal(after.state.worldSimulation.id,'existing-world');assert.deepEqual(after.state.profile,before.state.profile);assert.deepEqual(after.state.journal,before.state.journal);assert.ok(data.has(SAVE_KEY))});
